[tool.ruff]
target-version = "py310"
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.ruff.lint.per-file-ignores]
# 테스트 파일: mock 모듈 설정 후 import 필요
"tests/*.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
